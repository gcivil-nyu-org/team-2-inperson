//import axios from "axios";

export const assignSchoolToList = (studentId, schoolId, listId) => {
  console.log("Called AssignSchool:", {
    studentId: studentId,
    schoolId: schoolId,
    listId: listId,
  });
};
