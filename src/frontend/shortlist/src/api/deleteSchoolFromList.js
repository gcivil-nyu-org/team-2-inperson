//import axios from "axios";

export const deleteSchoolFromList = (studentId, schoolId, listId) => {
    console.log("Called AssignSchool:", {
      studentId: studentId,
      schoolId: schoolId,
      listId: listId,
    });
  };